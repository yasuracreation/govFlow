// global.d.ts
// Icons will be imported using ES Modules, so no need for window.LucideReact.

// Export something to make it a module, if it's not already.
// If this file is treated as a script, the global declaration works.
// If it's a module, it needs an export. An empty export is fine.
export {};
